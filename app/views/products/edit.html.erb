<div class="sticky top-0 bg-gray-50 flex justify-between">
  <h1 class="text-3xl text-gray-900 tracking-wide"><%= @product.name %></h1>
  <div class="flex">
    <section id='<%= dom_id(@product, :published_button) %>'>
      <%= render 'products/parts/published', product: @product %>
    </section>
    <%= link_to 'Save changes', '', class: 'ms-2 flex w-full justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2' %>
  </div>
</div>
